#pragma once

// Placeholder -- replay-mode avatar rendering hooks go here (e.g. hooking into
// whichever replay mod's playback API is targeted -- Beatleader/ScoreSaber replay
// tooling, or song-control). Left unimplemented in the initial scaffold.

namespace QavatarsRevived {
    void InitReplaySupport();
}
